#include "interpreter.h"

int main(int argc, const char * argv[]) {
    Run();
//    int i = 0;
//    fprintf(stderr, "%d", isdigit('\n'));
//    while(1){
//        Token& token = GetTok();
//        fprintf(stderr, "%d, %d\n", token.type, i++);
//    }
    return 0;
}
